```
程序说明:
    1.运行main函数即可。
    2.可修改main函数里面传给ReadAndWriteData函数的参数

```

```
3，（编程，可以选择不作答）编写一个函数实现数据的读取或存储到文件功能，要求： 
一，文件名（包含路径）及数据都由函数参数提供；
二，如果文件不存在，把数据写入文件中，返回空数据；
三，如果文件中有数据，则返回旧数据，并使用新数据覆盖原来的数据；
四，每次数据长度大于8byte
```